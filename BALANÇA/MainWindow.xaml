<Window x:Class="BALANÇA.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BALANÇA"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Sistema de Balança" Height="720" Width="1280" WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent">

    <Window.Resources>
        <Style x:Key="MenuRadioButtonStyle" TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#a9b1bd"/>
            <Setter Property="Padding" Value="20,15"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Grid>
                            <Border Name="background" Background="{TemplateBinding Background}" CornerRadius="8" Margin="10,2"/>
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                            <materialDesign:PackIcon Kind="ChevronRight" Foreground="White" 
                                                     HorizontalAlignment="Right" VerticalAlignment="Center" 
                                                     Visibility="Collapsed" Name="indicator"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="background" Property="Background" Value="{DynamicResource PrimaryHueDarkBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter TargetName="indicator" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="#f5f7fa" CornerRadius="10" BorderBrush="#dcdcdc" BorderThickness="1" MouseLeftButtonDown="Border_MouseLeftButtonDown">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#1e2129" CornerRadius="10,0,0,10">
                <StackPanel>
                    <Image Source="https://i.imgur.com/zVwA03p.png" Width="50" Height="50" VerticalAlignment="Top" Margin="0,20,0,0"/>
                    <TextBlock Text="Balança - Controladoria" Foreground="White" Opacity="0.5" TextWrapping="Wrap" TextAlignment="Center" Margin="5"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Background="#282c37">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Row="0" Margin="0,20,0,0">
                        <RadioButton x:Name="RecebimentoRadio" Style="{StaticResource MenuRadioButtonStyle}" GroupName="Menu" IsChecked="True" Click="MenuButton_Click"/>
                        <RadioButton x:Name="CarregamentoRadio" Style="{StaticResource MenuRadioButtonStyle}" GroupName="Menu" Click="MenuButton_Click"/>
                    </StackPanel>
                    <RadioButton x:Name="ConfiguracoesRadio" Grid.Row="1" Style="{StaticResource MenuRadioButtonStyle}" GroupName="Menu" Click="MenuButton_Click"/>
                </Grid>
            </Border>

            <Grid Grid.Column="2">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,5,5,0" Panel.ZIndex="1">
                    <Button Style="{StaticResource MaterialDesignFlatButton}" Click="MinimizeButton_Click" Content="—" ToolTip="Minimizar"/>
                    <Button Style="{StaticResource MaterialDesignFlatButton}" Click="MaximizeButton_Click" Content="[ ]" ToolTip="Maximizar"/>
                    <Button Style="{StaticResource MaterialDesignFlatButton}" Click="CloseButton_Click" Content="X" ToolTip="Fechar"/>
                </StackPanel>
                <Frame x:Name="MainFrame" NavigationUIVisibility="Hidden"/>
            </Grid>
        </Grid>
    </Border>
</Window>